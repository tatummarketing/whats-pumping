TATUM_API_KEY=your_tatum_api_key_here

# Comma-separated Host/Origin allowlist for /api/* proxies
API_ALLOWED_HOSTS=apps.tatum.io,whats-pumping.webflow.io,localhost,127.0.0.1

# Best-effort per-IP rate limits (override per-route defaults in code)
# API_RATE_LIMIT=60
# API_RATE_WINDOW_MS=60000
